name: peforming rust project checkout 

on: [push]
env:
  CARGO_TERM_COLOR: always

jobs:
  rust-project:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository 
        uses: actions/checkout@v3
      - name: Build
        run: cargo build --verbose
    